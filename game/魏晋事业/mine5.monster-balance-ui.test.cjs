const test=require('node:test'),assert=require('node:assert/strict'),path=require('node:path'),{pathToFileURL}=require('node:url'),{chromium}=require(process.env.PLAYWRIGHT_MODULE||'C:/Users/Million/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
test('inspection displays real names, fragile pack/swift traits, health and additive sneak bonus',async()=>{const browser=await chromium.launch({headless:true,executablePath:process.env.BROWSER_EXECUTABLE||'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'});try{const p=await browser.newPage({viewport:{width:390,height:844},reducedMotion:'reduce'}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.addInitScript(()=>{let Engine;Object.defineProperty(window,'EscapeEngine',{get(){return Engine;},set(E){Engine=new Proxy(E,{construct(T,args){const g=Reflect.construct(T,[12345]);window.__testGame=g;return g;}});}});});await p.goto(pathToFileURL(process.env.GAME_HTML||path.join(__dirname,'mine5.html')).href);await p.evaluate(()=>{const g=__testGame;g.startExpedition(g.campaign.preparedIds);g.unpackKit(g.state.expedition.cards.find(c=>c.id==='kit').uid);const s=g.state,m=s.monsters.find(m=>m.type==='bat'&&m.swarm);if(!m)throw Error('generated echo bat pack missing');s.mode='playing';s.player.x=30;s.player.y=30;s.player.facing=[1,0];s.monsters=[m];m.x=31;m.y=30;s.params.hazardStart=999999;for(let y=28;y<=32;y++)for(let x=28;x<=32;x++)Object.assign(g.tile(x,y),{kind:'floor',fog:null,items:{},seen:true});s.environment.clouds=[];s.visionCache=null;s.lightCache=null;});await p.locator('#world').focus();await p.keyboard.press('c');let text=await p.locator('#dialog').innerText();assert.match(text,/回声蝠/);assert.match(text,/群居 · 3只一群/);assert.match(text,/迅捷/);assert.match(text,/一次有效武器命中/);await p.keyboard.press('Escape');await p.evaluate(()=>{const g=__testGame;g.state.monsters=[g.createMonster('skeleton',31,30,999,[1,0])];});await p.locator('#world').focus();await p.keyboard.press('c');text=await p.locator('#dialog').innerText();assert.match(text,/骷髅/);assert.match(text,/剩余 3 点生命/);assert(!text.includes('迅捷先手'));await p.keyboard.press('Escape');await p.evaluate(()=>{const g=__testGame;g.state.monsters=[g.createMonster('undead',31,30,998,[1,0])];});await p.locator('#world').focus();await p.keyboard.press('c');text=await p.locator('#dialog').innerText();assert.match(text,/偷袭伤害\+1/);assert(!text.includes('偷袭一击'));assert.deepEqual(errors,[]);assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await p.screenshot({path:path.join(process.env.TEMP,'mine5-monster-inspection-390.png'),fullPage:true});}finally{await browser.close();}});
