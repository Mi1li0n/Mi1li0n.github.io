const test=require('node:test'),assert=require('node:assert/strict'),path=require('node:path'),{pathToFileURL}=require('node:url'),{chromium}=require(process.env.PLAYWRIGHT_MODULE||'C:/Users/Million/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
test('elite equipment active and lamp placement work through actual UI; six canvas models render',async()=>{const browser=await chromium.launch({headless:true,executablePath:process.env.BROWSER_EXECUTABLE||'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'});try{const p=await browser.newPage({viewport:{width:1280,height:900},reducedMotion:'reduce'}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.addInitScript(()=>{let Engine;Object.defineProperty(window,'EscapeEngine',{get(){return Engine;},set(E){Engine=new Proxy(E,{construct(T,args){const g=Reflect.construct(T,[12345]);window.__testGame=g;return g;}});}});});await p.goto(pathToFileURL(process.env.MINE5_HTML||path.join(__dirname,'mine5.html')).href);await p.evaluate(()=>{const g=__testGame;g.startExpedition(g.campaign.preparedIds);g.unpackKit(g.state.expedition.cards.find(c=>c.id==='kit').uid);const s=g.state;s.mode='playing';s.monsters=[];s.sites=[];s.params.vision=0;s.params.hazardStart=999999;s.player.x=30;s.player.y=30;s.player.facing=[1,0];s.environment.clouds=[];s.environment.cleared=[];for(const t of s.map.tiles){delete t.fixedLamp;t.torch=false;t.light=null;delete t.fog;}for(let y=20;y<=40;y++)for(let x=20;x<=40;x++)Object.assign(g.tile(x,y),{kind:'floor',collapseAt:null,gate:false,riskPassage:false});g.receiveCard(g.makeCard('elite_lantern'));g.equipGear('off','elite_lantern');const d=document.querySelector('#dialog');if(d.open)d.close();s.lightCache=null;s.visionCache=null;});await p.locator('#equipment-button').click();assert.match(await p.locator('#dialog-content').innerText(),/余烬矿灯/);assert.equal(await p.locator('[data-elite-active]').count(),0);assert.equal(await p.locator('[data-light-place="elite_lantern"]').count(),1);const before=await p.evaluate(()=>({turn:__testGame.state.turn,dur:__testGame.state.player.inventoryGear.elite_lantern.dur}));await p.locator('#dialog [data-action=close]').click();if(!await p.locator('#card-dock').isVisible())await p.locator('#card-tab').click();await p.locator('[data-ability-card=elite_lantern]').click();await clickCell(p,31,30);await p.locator('#confirm-aim').click();await p.waitForFunction(n=>__testGame.state.turn===n+1,before.turn);assert.equal(await p.evaluate(()=>__testGame.state.player.inventoryGear.elite_lantern.dur),before.dur-6);await p.locator('#equipment-button').click();const fuel=await p.evaluate(()=>__testGame.state.player.inventoryGear.elite_lantern.dur);await p.locator('[data-light-place="elite_lantern"]').click();await p.waitForFunction(()=>!__testGame.state.player.inventoryGear.elite_lantern);const placed=await p.evaluate(()=>{const g=__testGame;return {light:g.tile(30,30).light,off:g.state.player.equipped.off,card:g.state.expedition.cards.some(c=>c.id==='elite_lantern')};});assert.equal(placed.light.dur,fuel*2-1);assert.equal(placed.light.id,'elite_lantern');assert.equal(placed.off,null);assert.equal(placed.card,false);
 await p.evaluate(()=>{const g=__testGame,s=g.state;document.querySelector('#dialog').close();s.monsters=[];const coords=[[28,29],[30,28],[32,29],[28,31],[30,32],[32,31]];Object.keys(EscapeEngine.ELITES).forEach((type,i)=>{const m=g.createMonster(type,...coords[i],500+i);m.elite=true;m.cycle=0;s.monsters.push(m);});s.lightCache=null;s.visionCache=null;});await p.screenshot({path:path.join(process.env.TEMP,'mine5-elites-ui.png'),fullPage:true});assert.deepEqual(errors,[]);
 }finally{await browser.close();}});
test('using a selected three-dose tide card preserves another one-dose card',async()=>{const browser=await chromium.launch({headless:true,executablePath:process.env.BROWSER_EXECUTABLE||'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'});try{const p=await browser.newPage({viewport:{width:1280,height:900},reducedMotion:'reduce'}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.addInitScript(()=>{let Engine;Object.defineProperty(window,'EscapeEngine',{get(){return Engine;},set(E){Engine=new Proxy(E,{construct(T,args){const g=Reflect.construct(T,[12345]);window.__testGame=g;return g;}});}});});await p.goto(pathToFileURL(process.env.MINE5_HTML||path.join(__dirname,'mine5.html')).href);const ids=await p.evaluate(()=>{const g=__testGame;g.startExpedition(g.campaign.preparedIds);g.unpackKit(g.state.expedition.cards.find(c=>c.id==='kit').uid);const s=g.state;s.mode='playing';s.monsters=[];s.sites=[];s.params.hazardStart=999999;const full=g.makeCard('elite_tide',3),small=g.makeCard('elite_tide',1);g.receiveCard(full);g.receiveCard(small);document.querySelector('#dialog').close();return {full:full.uid,small:small.uid,turn:s.turn};});await p.locator('#equipment-button').click();await p.locator('#dialog [data-action=close]').click();if(!await p.locator('#card-dock').isVisible())await p.locator('#card-tab').click();const selected=p.locator('[data-card-select="'+ids.full+'"]');await selected.scrollIntoViewIfNeeded();await clickCard(p,ids.full);assert.match(await p.locator('#card-popover').innerText(),/潮汐囊/);await p.locator('[data-card-use="'+ids.full+'"]').click();await p.waitForFunction(n=>__testGame.state.turn===n+1,ids.turn);const left=await p.evaluate(ids=>{const cs=__testGame.state.expedition.cards;return {full:cs.find(c=>c.uid===ids.full)?.qty,small:cs.find(c=>c.uid===ids.small)?.qty};},ids);assert.deepEqual(left,{full:2,small:1});assert.deepEqual(errors,[]);}finally{await browser.close();}});

async function clickCell(p,x,y){const q=await p.evaluate(({x,y})=>{const c=document.querySelector('#world'),r=c.getBoundingClientRect(),a=__testGame.state.player;return{x:r.x+r.width/2+(x-a.x)*80*r.width/c.width,y:r.y+r.height/2+(y-a.y)*80*r.height/c.height};},{x,y});await p.mouse.click(q.x,q.y);}
async function clickCard(p,uid){const q=await p.evaluate(uid=>{const b=document.querySelector('[data-card-select="'+uid+'"]'),r=b.getBoundingClientRect();for(let y=Math.max(1,r.y+4);y<Math.min(innerHeight,r.bottom-2);y+=5)for(let x=Math.max(1,r.x+4);x<Math.min(innerWidth,r.right-2);x+=4)if(document.elementFromPoint(x,y)?.closest('[data-card-select]')===b)return{x,y};},uid);assert(q);await p.mouse.click(q.x,q.y);}
