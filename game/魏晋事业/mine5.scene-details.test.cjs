const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const html=fs.readFileSync(process.env.MINE5_HTML||'D:/MY_Blog/source/game/魏晋事业/mine5.html','utf8'),ctx=vm.createContext({console});for(const m of html.matchAll(/<script\b[^>]*>([\s\S]*?)<\/script>/g))if(!m[1].includes('canvas=$'))vm.runInContext(m[1],ctx);for(let n=0;n<3;n++){const l=ctx.EscapeEngine.GEN.layoutChapter(17,n,[]);for(const r of l.regions.slice(9)){const count=l.tiles.filter(t=>t.kind==='floor'&&t.region===r.index&&t.sceneDetail===r.role).length;assert.ok(count>=4,r.role+' visible decorations '+count)}}assert.ok(html.includes('sceneDetail(x,y,t.sceneDetail)'));console.log('All 13 extra scenes have reachable-theme decorations and renderer.');
